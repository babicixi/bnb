node_modules
dist
coverage
data
uploads
.env
!/.env.example
*.log
.idea
.venv
*.iml
~$*
# Claude Code workspace state — contains local prompts/settings, never share
.claude/
# Personal/working docs that don't belong in the public repo
*.docx
# Local credentials (API tokens for Render, Cloudflare, GitHub). NEVER commit.
.local/
